{
  "name": "medal-js",
  "description": "JavaScript library for the Medal.tv API",
  "version": "0.0.0",
  "main": "dist/index.js",
  "repository": "https://github.com/pqt/medal-js",
  "author": "Austin Paquette <austin.paquette@forcir.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "echo \"Pretend to run the build script\"; exit 0",
    "format": "echo \"Pretend to run the format script\"; exit 0",
    "lint": "echo \"Pretend to run the lint script\"; exit 0",
    "test": "echo \"Pretend to run the test script\"; exit 0"
  }
}
